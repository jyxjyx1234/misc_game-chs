::START


# スプライト設定
ch(1, POS, 118, 0)	# 立ちキャラ表示用
ch(2, POS, 118, 0)	# 立ちキャラ表示用
ch(3, POS, 118, 0)	# 立ちキャラ表示用
ch(4, POS, 118, 0)	# エフェクト表示用
ch(5, POS, 118, 0)	# エフェクト表示用
ch(6, POS, 118, 0)	# エフェクト表示用
ch(7, POS, 118, 0)	# チップ表示用
ch(8, POS, 118, 0)	# チップ表示用
ch(9, POS, 118, 0)	# チップ表示用

ch(10, POS, 118, 0)	# チップ表示用
ch(11, POS, 118, 0)	# チップ表示用
ch(12, POS, 118, 0)	# チップ表示用
ch(13, POS, 118, 0)	# チップ表示用
ch(14, POS, 118, 0)	# チップ表示用
ch(15, POS, 118, 0)	# チップ表示用
ch(16, POS, 118, 0)	# チップ表示用
ch(17, POS, 118, 0)	# チップ表示用
ch(18, POS, 118, 0)	# チップ表示用
ch(19, POS, 118, 0)	# チップ表示用
ch(20, POS, 118, 0)	# チップ表示用
ch(21, POS, 118, 0)	# チップ表示用
ch(22, POS, 118, 0)	# チップ表示用
ch(23, POS, 118, 0)	# チップ表示用
ch(24, POS, 118, 0)	# チップ表示用
ch(25, POS, 118, 0)	# チップ表示用

ch(26, POS, 118, 0)	# チップ表示用
ch(27, POS, 118, 0)	# フェイスウインドウセル表示用
ch(28, POS, 118, 0)	# フェイスウインドウ表示用
ch(29, POS, 118, 0)	# フェイスウインドウ表示用


ch(26, SYS, 2)	# システムスプライト指定フラグとなる。（0 or 1 or 2）■このスプライトは選択肢時のメッセージウインドウ表示に使用
#ch(27, SYS, 2)	# システムスプライト指定フラグとなる。（0 or 1 or 2）
ch(27, SYS, 1)	# システムスプライト指定フラグとなる。（0 or 1 or 2）
ch(28, SYS, 1)	# システムスプライト指定フラグとなる。（0 or 1 or 2）
ch(29, SYS, 1)	# システムスプライト指定フラグとなる。（0 or 1 or 2）


ch(1, PNT, 1)	# 原点位置指定となる。（現在 x座標のみの対応）1 : 真ん中
ch(2, PNT, 1)	# 原点位置指定となる。（現在 x座標のみの対応）1 : 真ん中
ch(3, PNT, 1)	# 原点位置指定となる。（現在 x座標のみの対応）1 : 真ん中
ch(4, PNT, 1)	# 原点位置指定となる。（現在 x座標のみの対応）1 : 真ん中
ch(5, PNT, 1)	# 原点位置指定となる。（現在 x座標のみの対応）1 : 真ん中
ch(6, PNT, 1)	# 原点位置指定となる。（現在 x座標のみの対応）1 : 真ん中
ch(7, PNT, 1)	# 原点位置指定となる。（現在 x座標のみの対応）1 : 真ん中
ch(8, PNT, 1)	# 原点位置指定となる。（現在 x座標のみの対応）1 : 真ん中
ch(9, PNT, 1)	# 原点位置指定となる。（現在 x座標のみの対応）1 : 真ん中

ch(10, PNT, 1)	# 原点位置指定となる。（現在 x座標のみの対応）1 : 真ん中
ch(11, PNT, 1)	# 原点位置指定となる。（現在 x座標のみの対応）1 : 真ん中
ch(12, PNT, 1)	# 原点位置指定となる。（現在 x座標のみの対応）1 : 真ん中
ch(13, PNT, 1)	# 原点位置指定となる。（現在 x座標のみの対応）1 : 真ん中
ch(14, PNT, 1)	# 原点位置指定となる。（現在 x座標のみの対応）1 : 真ん中
ch(15, PNT, 1)	# 原点位置指定となる。（現在 x座標のみの対応）1 : 真ん中
ch(16, PNT, 1)	# 原点位置指定となる。（現在 x座標のみの対応）1 : 真ん中
ch(17, PNT, 1)	# 原点位置指定となる。（現在 x座標のみの対応）1 : 真ん中
ch(18, PNT, 1)	# 原点位置指定となる。（現在 x座標のみの対応）1 : 真ん中
ch(19, PNT, 1)	# 原点位置指定となる。（現在 x座標のみの対応）1 : 真ん中
ch(20, PNT, 1)	# 原点位置指定となる。（現在 x座標のみの対応）1 : 真ん中
ch(21, PNT, 1)	# 原点位置指定となる。（現在 x座標のみの対応）1 : 真ん中
ch(22, PNT, 1)	# 原点位置指定となる。（現在 x座標のみの対応）1 : 真ん中
ch(23, PNT, 1)	# 原点位置指定となる。（現在 x座標のみの対応）1 : 真ん中
ch(24, PNT, 1)	# 原点位置指定となる。（現在 x座標のみの対応）1 : 真ん中
ch(25, PNT, 1)	# 原点位置指定となる。（現在 x座標のみの対応）1 : 真ん中


^ret

::END
